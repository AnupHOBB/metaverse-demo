export function toSceneObject(token, color, position, rotation, dimension, actionType)
{
    return { token : token, color : color, position : position, rotation : rotation, dimension : dimension, actionType : actionType }
}